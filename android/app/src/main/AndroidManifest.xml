<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- The Flutter template only grants INTERNET in the debug and profile
         manifests, so a release build cannot reach the server at all. The app
         syncs with BMA-NFE, so it belongs here too. ACCESS_NETWORK_STATE backs
         the offline banner (connectivity_plus also declares it). -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <application
        android:label="BMA Mobile"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <!-- TABLET-FIRST. The app targets a 9-inch tablet in BOTH orientations
             (1280x800 landscape, 800x1280 portrait) and keeps the phone as a
             fallback, so there is deliberately NO android:screenOrientation,
             no android:maxAspectRatio and no <supports-screens> anywhere in the
             project: any of them would silently defeat the layout work.
             android:resizeableActivity is already the default at minSdk 24 and
             targetSdk 36 ignores size restrictions above 600dp, so declaring it
             is documentary — but it states the intent in review and still
             matters on older OEM 9-inch tablets in the field.
             The configChanges list below already covers orientation, screenSize
             and density, so rotating never recreates the activity: Flutter just
             receives a new view size. See docs/TABLET_LAYOUT.md. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:resizeableActivity="true"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
